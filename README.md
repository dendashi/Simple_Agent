# Simple_Agent

A_Chat_Agent 是一个简单的聊天 Agent ，在终端中输入 ```python -m uvicorn agent_bulid:app --reload``` 开始运行。

A_Object_Detection_Agent 是一个可以识别图片并完成目标检测任务的 Agent ，在终端中输入```python -m uvicorn server:app --reload``` 开始运行。
